/**
 * ClockPort - Interface for time operations
 * Pure interface definition - no implementation
 * @typedef {Object} ClockPort
 * @property {() => Date} now - Get current date/time
 * @property {() => number} nowMs - Get Unix timestamp in milliseconds
 */

export {};
