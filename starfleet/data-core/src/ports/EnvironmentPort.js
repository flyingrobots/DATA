/**
 * EnvironmentPort - Interface for environment variables
 * Pure interface definition - no implementation
 * @typedef {Object} EnvironmentPort
 * @property {(key: string) => string | undefined} get - Get environment variable
 * @property {(key: string) => boolean} has - Check if environment variable exists
 */

export {};