/**
 * GlobPort - Interface for file pattern matching
 * Pure interface definition - no implementation
 * @typedef {Object} GlobPort
 * @property {(patterns: string[], opts?: {cwd?: string, ignore?: string[], dot?: boolean}) => Promise<string[]>} find - Find files matching patterns
 */

export {};
